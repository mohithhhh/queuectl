# queuectl
QueueCTL is a CLI-based background job queue system that supports enqueuing jobs, running multiple workers, automatic retries with exponential backoff, and a Dead Letter Queue. Built with persistent storage, it ensures reliability, concurrency safety, and configurable execution with options like priority and scheduling.
